import { Options } from './options.interface.js';

/**
 * Encodes content to base64 and creates a new file with this encoded text.
 *
 * @param pathToFile
 * @param options
 */
function encodeToBase64(pathToFile: string, options?: Options): void {

}

const options: Options = {
  replace: 'ddd',
};

console.log('test', options);
